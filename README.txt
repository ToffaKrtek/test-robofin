Запуск:

-	При использовании Docker-compose, команда 'docker-compose up' (зарезервированный порт 8080 => http://localhost:8080)


-	Для развертывания на хостинге, прописать в  хост, порт, имя пользователя БД, 

Создание Базы Данных:

-	При использовании Docker-compose, происходит самостоятельно (все обращения прописаны именно под данное использование)

        В ходе тестирования запуска в 1 из 5 раз инициализации получил неполную загрузку данных (mysql/docker-entrypoint-initdb.d/2-pull-data.sql) 
        - вообще для подобной загрузки из локальных файлов (CSV решил оставить как есть, не перегоняя в sql, используя только предложенное в ТЗ 
        //только добавил создание БД в файл create_tables.sql для инициализации в mysql/docker-entrypoint-initdb.d/1-init.sql)

        Альтервное создание таблиц и их заполнение через скрипт createdb.php (localhost:8080/createdb.php) но его протестировать не успел.
        Так же данные можно добавить через СУБД localhost:8090/ (host: mysqldb, user: root, password: password, database: robofin), 
        CSV-файлы лежат в mysql/docker-entrypoint-initdb.d/pull-data и в src/data 
        -- если я всеже не учел каких-то зависимостей стоит импортировать их вручную (разделитель ';')

-	Для развертывания на хостинге, реализована альтернатива - php-скрипт (dbcreate.php), 
    необходимо (после выполнения необходимых действий из инструкции по Запуску) открыть страницу {domen_name}/dbcreate.php
